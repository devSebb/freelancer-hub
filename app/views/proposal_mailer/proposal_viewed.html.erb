<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; border-bottom: 1px solid #e5e7eb; }
    .content { padding: 30px 0; }
    .info-badge { background: #dbeafe; color: #1e40af; padding: 4px 12px; border-radius: 9999px; font-size: 14px; font-weight: 500; display: inline-block; margin-bottom: 16px; }
    .info-box { background: #f9fafb; border: 1px solid #e5e7eb; border-radius: 8px; padding: 16px; margin: 20px 0; }
    .info-row { margin-bottom: 8px; }
    .info-label { color: #6b7280; font-size: 14px; }
    .info-value { font-weight: 500; }
    .button { display: inline-block; background: #2563eb; color: white !important; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: 500; margin: 20px 0; }
    .footer { text-align: center; padding-top: 20px; border-top: 1px solid #e5e7eb; color: #6b7280; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0; color: #111;">👀 <%= t("mailers.proposal_viewed.greeting") %></h1>
    </div>

    <div class="content">
      <span class="info-badge">Viewed</span>

      <p><%= t("mailers.proposal_viewed.intro", title: @proposal.title) %></p>

      <div class="info-box">
        <div class="info-row">
          <div class="info-label"><%= t("mailers.proposal_viewed.client_name") %></div>
          <div class="info-value"><%= @client&.name || "—" %></div>
        </div>
        <div class="info-row">
          <div class="info-label"><%= t("mailers.proposal_viewed.viewed_at") %></div>
          <div class="info-value"><%= @proposal.viewed_at&.strftime("%B %d, %Y at %I:%M %p") %></div>
        </div>
      </div>

      <a href="<%= proposal_url(@proposal) %>" class="button"><%= t("mailers.proposal_accepted.view_proposal") %></a>
    </div>

    <div class="footer">
      <p><%= t("app_name") %></p>
    </div>
  </div>
</body>
</html>

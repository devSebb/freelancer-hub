<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', -apple-system, BlinkMacSystemFont, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; border-bottom: 1px solid #e5e7eb; }
    .content { padding: 30px 0; }
    .success-badge { background: #e0fbe7; color: #1a5c2e; padding: 4px 12px; border-radius: 9999px; font-size: 14px; font-weight: 500; display: inline-block; margin-bottom: 16px; }
    .signature-box { background: #f9fafb; border: 1px solid #e5e7eb; border-radius: 8px; padding: 16px; margin: 20px 0; }
    .signature-row { display: flex; justify-content: space-between; margin-bottom: 8px; }
    .signature-label { color: #6b7280; font-size: 14px; }
    .signature-value { font-weight: 500; }
    .button { display: inline-block; background: #94F7AD; color: #111 !important; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: 500; margin: 20px 0; }
    .footer { text-align: center; padding-top: 20px; border-top: 1px solid #e5e7eb; color: #6b7280; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0; color: #111;">🎉 <%= t("mailers.proposal_accepted.greeting") %></h1>
    </div>

    <div class="content">
      <span class="success-badge">✓ Accepted</span>

      <p><%= t("mailers.proposal_accepted.intro", title: @proposal.title) %></p>

      <div class="signature-box">
        <div style="margin-bottom: 12px;">
          <div class="signature-label"><%= t("mailers.proposal_accepted.signed_by") %></div>
          <div class="signature-value"><%= @proposal.signature_name %></div>
        </div>
        <div>
          <div class="signature-label"><%= t("mailers.proposal_accepted.signed_at") %></div>
          <div class="signature-value"><%= @proposal.signature_at&.strftime("%B %d, %Y at %I:%M %p") %></div>
        </div>
      </div>

      <p><%= t("mailers.proposal_accepted.next_steps") %></p>

      <a href="<%= proposal_url(@proposal) %>" class="button"><%= t("mailers.proposal_accepted.view_proposal") %></a>
    </div>

    <div class="footer">
      <p><%= t("app_name") %></p>
    </div>
  </div>
</body>
</html>
